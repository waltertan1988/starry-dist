version: '3.x'

services:
  redis-stack:
    image: redis/redis-stack:latest
    container_name: redis-stack
    hostname: redis-stack
    ports:
      - "6379:6379"
      - "8001:8001"
    volumes:
      - ./data/redis:/data
    networks:
      - starry
